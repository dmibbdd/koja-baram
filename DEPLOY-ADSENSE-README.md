# کجا برم (KAJA BERAM) — راهنمای انلاین کردن و کسب درآمد با گوگل ادسنس

این سند مسیر کامل رو از «فایل روی سیستمت» تا «سایت زنده با تبلیغ گوگل» توضیح می‌ده.

---

## ۱. پیش‌نیازها

- یک حساب [GitHub](https://github.com) (رایگان)
- یک حساب [Vercel](https://vercel.com) یا [Netlify](https://netlify.com) (رایگان — با گیت‌هاب وارد شو)
- Node.js نسخه‌ی ۲۰ به بالا روی سیستمت (برای تست محلی)

---

## ۲. تست محلی قبل از انتشار

```bash
npm install
npm run dev
```

سایت روی `http://localhost:8443` بالا میاد. مطمئن شو همه‌چیز درست کار می‌کنه، به‌خصوص:
- صفحه‌ی `/privacy` (حریم خصوصی) درست نمایش داده می‌شه
- سوییچ زبان فارسی/انگلیسی کار می‌کنه
- عکس‌های `site-img` درست لود می‌شن

برای تست خروجی نهایی (همون چیزی که آنلاین می‌شه):

```bash
npm run build
npm run preview
```

---

## ۳. انتشار رایگان (Vercel یا Netlify)

### مرحله به مرحله:

1. کد رو در یک ریپازیتوری تازه روی GitHub push کن.
2. وارد [vercel.com](https://vercel.com) (یا netlify.com) شو و با گیت‌هابت وارد شو.
3. گزینه‌ی «New Project» → ریپازیتوری پروژه رو انتخاب کن.
4. چون فریم‌ورک Vite تشخیص داده می‌شه، تنظیمات build خودکار پر می‌شه:
   - Build Command: `npm run build` یا `vite build`
   - Output Directory: `dist`
5. روی Deploy بزن. چند دقیقه بعد یک آدرس رایگان مثل `koja-baram.vercel.app` داری.
6. از این به بعد، هر بار که به شاخه‌ی اصلی push کنی، سایت خودکار دوباره ساخته و آپدیت می‌شه.

### دامنه‌ی اختصاصی (اختیاری، معمولاً چند دلار در سال)

اگه دلت می‌خواد به‌جای `koja-baram.vercel.app` یک دامنه‌ی واقعی مثل `kojabaram.com` داشته باشی:
1. دامنه رو از جایی مثل Namecheap یا یک ثبت‌کننده‌ی داخلی بخر.
2. در تنظیمات پروژه در Vercel/Netlify، بخش Domains رو باز کن و دامنه رو اضافه کن.
3. رکوردهای DNS که نشونت می‌ده رو نزد ثبت‌کننده‌ی دامنه وارد کن (معمولاً یک رکورد A یا CNAME).

دامنه‌ی اختصاصی نه‌فقط حرفه‌ای‌تره، بلکه معمولاً تأیید گوگل ادسنس رو هم راحت‌تر می‌کنه.

---

## ۴. آماده‌سازی برای گوگل ادسنس

قبل از درخواست، این‌ها رو چک کن:

- ✅ **صفحه‌ی حریم خصوصی** — از قبل در `/privacy` اضافه شده (دوزبانه، شامل بخش کوکی‌های تبلیغاتی گوگل).
- ✅ **محتوای کافی** — گوگل سایت‌های خیلی خالی یا کپی‌شده رو رد می‌کنه. مقصدهای بیشتر، توضیحات بیشتر و صفحات راهنما (Guides) به شانس تأیید کمک می‌کنه.
- ⚠️ **مالکیت عکس‌ها** — پوشه‌ی `site-img` رو چک کن. اگه عکس‌ها از منابع بدون لایسنس آزاد اومدن، جایگزینشون کن با عکس‌های [Unsplash](https://unsplash.com) یا [Pexels](https://pexels.com) که رایگان و بدون کپی‌رایت هستن.
- ✅ **صفحه‌ی «درباره‌ی ما» و «تماس با ما»** — در `/about` و `/contact` اضافه شدن. **مهم:** ایمیل placeholder توی `src/pages/ContactPage.tsx` (متغیر `CONTACT_EMAIL`) رو با ایمیل واقعی خودت جایگزین کن.
- 🔲 **کمی ترافیک واقعی** — لازم نیست خیلی زیاد باشه، ولی یک سایت کاملاً بدون بازدید شانس کمتری داره.

---

## ۵. فعال‌سازی گوگل ادسنس

1. به [ads.google.com/home](https://adsense.google.com) برو و با آدرس سایت دیپلوی‌شده‌ات ثبت‌نام کن.
2. گوگل یک کد اسکریپت تأیید بهت می‌ده — این کد رو باید در `index.html` (داخل `<head>`) قرار بدی.
3. منتظر بررسی بمون (معمولاً چند روز تا چند هفته).
4. بعد از تأیید:
   - در فایل `src/components/AdSlot.tsx`، مقدار `ADSENSE_CLIENT` رو با Publisher ID واقعی‌ات (چیزی شبیه `ca-pub-1234567890123456`) جایگزین کن.
   - در همون فایل، `SHOW_REAL_ADS` رو به `true` تغییر بده.
   - برای هر اسلات (در `HomePage.tsx`، `AttractionPage.tsx`، `CityPage.tsx`) مقدار `slot="..."` رو با Slot ID واقعی همون Ad Unit که در پنل ادسنس ساختی جایگزین کن.

تا وقتی این مراحل رو انجام ندی، به‌جای تبلیغ واقعی یک باکس ساده با نوشته‌ی «جای تبلیغ» نمایش داده می‌شه — این عمداً هست، چون نمایش تبلیغ واقعی قبل از تأیید ادسنس، خلاف قوانین گوگله.

### محل اسلات‌های تبلیغ فعلی

| صفحه | تعداد | موقعیت |
|---|---|---|
| صفحه‌ی اصلی | ۲ | بعد از «مقاصد پرطرفدار» و بعد از «جاذبه‌های برتر» |
| صفحه‌ی جاذبه | ۱ | بعد از توضیحات، قبل از جدول جزئیات |
| صفحه‌ی شهر | ۱ | بعد از گرید جاذبه‌ها |

می‌تونی هر جای دیگه‌ای هم که خواستی، با کپی کردن `<AdSlot variant="..." slot="..." />` اضافه کنی. سه حالت داره: `leaderboard` (باریک و پهن)، `rectangle` (مربعی)، `in-feed` (بین محتوا).

---

## ۶. چک‌لیست خلاصه

- [ ] تست محلی با `npm run dev`
- [ ] Push به GitHub
- [ ] دیپلوی روی Vercel یا Netlify
- [ ] (اختیاری) اتصال دامنه‌ی اختصاصی
- [ ] بررسی مالکیت عکس‌ها
- [ ] جایگذاری ایمیل واقعی در `src/pages/ContactPage.tsx`
- [ ] درخواست گوگل ادسنس با آدرس زنده‌ی سایت
- [ ] بعد از تأیید: فعال کردن `SHOW_REAL_ADS` و جایگذاری ID های واقعی در `AdSlot.tsx`

موفق باشی! 🌍
